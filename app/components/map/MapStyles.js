import { StyleSheet } from "react-native";

const mapStyles = StyleSheet.create({
  mapStyle: {
    width: "auto",
    height: "30%",
    zIndex: -1,
    borderTopWidth: 10,
  },
});

export default mapStyles;
